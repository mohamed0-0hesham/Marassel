# ── Android ───────────────────────────────────────────────────────────────────
*.iml
.gradle/
local.properties
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
*.apk
*.aab
*.ap_

# ── Android Studio ────────────────────────────────────────────────────────────
/.idea/
*.iws
*.ipr

# ── SECURITY — Never commit these ─────────────────────────────────────────────
# Firebase — contains API keys and project config
google-services.json

# Signing keystores
*.jks
*.keystore
keystore.properties
release.keystore
debug.keystore

# Environment / secrets files
*.env
secrets.properties
local.defaults.properties

# ── Build outputs ─────────────────────────────────────────────────────────────
app/build/
**/build/

# ── Kotlin ────────────────────────────────────────────────────────────────────
*.class

# ── Gradle ────────────────────────────────────────────────────────────────────
.gradle
gradle-app.setting
!gradle-wrapper.jar
!gradle-wrapper.properties

# ── macOS ─────────────────────────────────────────────────────────────────────
.AppleDouble
.LSOverride