# Marassel
A simple Chat Application that allows users to send and receive messages in real time using Firebase as the backend. The app should support text and media messages, manage background tasks for sending, and provide user-visible notifications when background work is in progress (e.g., sending messages or uploading media).
